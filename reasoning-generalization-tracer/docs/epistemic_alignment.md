# Epistemic Alignment Reward Foundation

This document records the V1/V2 foundation for RG-Tracer's abstention and
thought-alignment rewards. The schema has case 0 as fallback, cases 1-8 as
answer cases, and cases 9-13 as IDK/abstention cases. Thought rewards are paid
only for eligible aligned cases and are never negative.
